# docs/etl Residues (moved)

These files were moved out of `docs/etl/` during cleanup on 2026-02-16.

Why moved:
- They were legacy pointer/deprecated stubs.
- Canonical artifacts already exist under `docs/etl/sprints/<sprint_id>/...`.
- Active docs were rewritten to canonical paths.

If any file is still needed, restore it from this folder.
