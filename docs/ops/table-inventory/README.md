# Table Inventory Audits

CSV snapshots from schema/table inventory checks.

These are operational audit artifacts, not source code. Keep future table inventory outputs here instead of the repo root.
