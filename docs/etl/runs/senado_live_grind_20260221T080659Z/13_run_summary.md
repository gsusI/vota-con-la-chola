# Senado Live Grind Summary (2026-02-21)

Run ID:
- `senado_live_grind_20260221T080659Z`

Mode:
- 12 rounds of:
  - skip-wide pass (`--include-unlinked --limit-initiatives 4000`, no `--retry-forbidden`)
  - conditional cookie retry probe (`--retry-forbidden --limit-initiatives 60`) when skip-wide had no fetchable URLs.

Outcome:
- Start: `5724/7905`
- End: `5724/7905`
- Net: `+0`

Round behavior:
- All 12 rounds reported `skip_ok=0`, `skip_fetch=0`, `retry_ok=0`.

Interpretation:
- Current queue remainder is in a hard blocked state in this environment/time window.
