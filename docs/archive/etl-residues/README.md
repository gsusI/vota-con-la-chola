# Archived ETL residues

These files were moved out of `docs/etl/` during cleanup on 2026-02-16, then moved here from the temporary deletion bucket during repo organization.

Why moved:
- They were legacy pointer/deprecated stubs.
- Canonical artifacts already exist under `docs/etl/sprints/<sprint_id>/...`.
- Active docs were rewritten to canonical paths.

Do not add new execution plans here. Update `ROADMAP.md`, `docs/roadmap-tecnico.md`, or `docs/etl/e2e-scrape-load-tracker.md` instead.
