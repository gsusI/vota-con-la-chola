"""ETL package namespace.

This repo uses `etl/` both as a data directory (etl/data/...) and as a Python package.
Keeping code under `etl/` allows `python3 scripts/...` to import it without extra packaging.
"""

