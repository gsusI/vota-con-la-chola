# publicdata_ops

Reusable operation helpers for public-data workflows.

Current scope:
- source queue state constants
- dependency ordering for source queue items
- command template rendering
- command normalization for DB and snapshot-date arguments

Vota still owns the concrete runner, subprocess policy, tracker semantics, and product reports.
